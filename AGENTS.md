# AI读书雷达 Agent 规则配置

## 1. 通用开发偏好 (General Preferences)

- 语言交互：AI 的回答与代码注释必须全部使用简体中文，解释力求简洁、逻辑严密。
- 代码生成：修改代码时，优先提供完整的、开箱即用的更新后文件，避免给凌乱的“代码碎片/插桩”。
- 绝无占位符：严禁在代码中写 `// TODO: implement later` 或 `// ...` 等占位注释，必须补齐完整业务逻辑。
- 优先保持输出结构化：标题、列表、表格、检查项。

## 2. 编码风格规范 (Coding Style)

- TypeScript：必须开启 strict 校验，严格进行类型约束，禁止滥用 `any`。
- 组织形式：函数必须保持单一职责，单个函数长度原则上不超过 50 行，复杂逻辑合理抽离。
- 模块导入：统一使用 ES Modules (`import/export`) 语法，禁止混用 CommonJS。

## 3. 提交规范 (Git Commits)

- Git commit message 必须使用标准语义格式：
  - `feat: [描述]` 新增功能。
  - `fix: [描述]` 修复 Bug。
  - `docs: [描述]` 文档变更。
  - `style: [描述]` 格式/样式调整。
